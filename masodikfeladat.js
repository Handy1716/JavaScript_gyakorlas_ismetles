function getRandomArbitrary(min, max) {
    return Math.round(Math.random() * (max - min) + min);
  }
let macskak = [];
let macsk = {nev: "Cirmos ", eletkor: getRandomArbitrary(2,10)};
for ( i = 0; i < 20 ; i++ ) {
    macskak[i].push(})
}